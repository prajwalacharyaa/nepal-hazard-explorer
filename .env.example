# Copy to .env (same folder) and fill in. .env is gitignored — never commit it.

# NASA Earthdata login bearer token, for pipeline/fetch_rain.py (IMERG rainfall).
# Get one: https://urs.earthdata.nasa.gov/profile  ->  Generate Token
# Also required once: same profile page -> Applications -> approve
#   "NASA GESDISC DATA ARCHIVE"  (else downloads 403).
EARTHDATA_TOKEN=
