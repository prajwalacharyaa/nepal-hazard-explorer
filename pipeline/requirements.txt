# core pipeline (fetch_* / clean_merge / aggregate / outlook / glof)
pandas>=2.2
numpy>=1.26
geopandas>=1.0
shapely>=2.0
requests>=2.32
python-dateutil>=2.9

# only for the optional experimental layers A and C:
#   susceptibility.py, fetch_rain.py, climate_context.py
rasterio>=1.3
rasterstats>=0.19
xarray>=2024.3
h5netcdf>=1.3
h5py>=3.11
netCDF4>=1.6
